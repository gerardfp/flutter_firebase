import 'package:flutter/material.dart';

import 'start_page.dart';

main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
   build(context) => MaterialApp(home: StartPage());
}